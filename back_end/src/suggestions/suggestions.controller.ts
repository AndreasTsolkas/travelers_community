import {
    Body,
    Controller,
    Post,
    Patch,
    HttpCode,
    HttpStatus,
    UseGuards,
    Get,
    UseInterceptors,
    UploadedFile,
    Headers,
  } from '@nestjs/common';
  
  
  @Controller('suggestions')
  export class SuggestionsController {
    constructor(
    ) {}
  
 
  }
  